diff --git a/src/app/demo-production-ready.js b/src/app/demo-production-ready.js
index 2f4e6d3..0000000 100644
--- a/src/app/demo-production-ready.js
+++ b/src/app/demo-production-ready.js
@@ -42,10 +42,6 @@
   // Demo components list for production showcase
-  // Legacy references removed by Phase 4 cleanup
-  'src/app/components/ui/premium-icon/premium-icon.component.ts',
-  'src/app/components/shared/human-message/human-message.component.ts',
-
-  // End of legacy references
-
   'src/app/components/ui/premium-card/premium-card.component.ts',
   'src/app/components/shared/human-activity-feed/human-activity-feed.component.ts',

diff --git a/src/app/tests/data-retention.spec.ts b/src/app/tests/data-retention.spec.ts
index 93af123..c5d42b2 100644
--- a/src/app/tests/data-retention.spec.ts
+++ b/src/app/tests/data-retention.spec.ts
@@ -15,11 +15,9 @@ describe('Data Retention Config', () => {
   let component: ConfiguracionComponent;

   beforeEach(() => {
-    component = new ConfiguracionComponent();
-    component.configSections = [
-      { id: 'privacy', label: 'Privacy Settings' },
-      { id: 'retention', label: 'Retention Policy' }
-    ];
+    component = new ConfiguracionComponent();
+    // Phase 4 cleanup: configSections removed, using updated API
+    // Adjust tests to use new getters or service mocks instead of component.configSections
   });

   it('should create the component', () => {
@@ -30,7 +28,9 @@ describe('Data Retention Config', () => {
   });

-  it('should have 2 config sections', () => {
-    expect(component.configSections.length).toBe(2);
-  });
+  it('should expose retention settings through service', () => {
+    // Phase 4 cleanup: adapt to new ConfiguracionService API
+    const retention = component.getRetentionSettings();
+    expect(retention).toBeDefined();
+  });
 });