# Legacy Snapshot Artifacts

Estos archivos se conservan únicamente como referencia histórica tras la migración a `web/`.

- `design-tokens.legacy.ts`: versión previa de los tokens de diseño.
- `voice-test.legacy.ts`: helper de consola utilizado para pruebas manuales del módulo de voz.

No deben importarse ni compilarse dentro del workspace actual. Para consultar el resto del snapshot legacy utiliza la rama/tag correspondiente en control de versiones.
